# Active Corpus

This is the current active corpus folder used by the app.

Current active files:

```text
drmadhupatil_enriched_rag_corpus.jsonl
metadata_enrichment_manifest.json
```

Configured in:

```text
backend/app/config.py
```

Future approved content should eventually be merged into this active corpus or the app config should be changed to point to the new merged RAG-ready corpus.

